CMAKE_MINIMUM_REQUIRED(VERSION 2.8.6)
PROJECT(PluginTest)

ENABLE_TESTING(TRUE)
SET(QT_USE_QTTEST TRUE)

SET(PLUGINTEST plugintest.cpp)

QT4_AUTOMOC(${PLUGINTEST})
ADD_EXECUTABLE(plugintest ${PLUGINTEST})

TARGET_LINK_LIBRARIES(plugintest ${STATICLIB} ${TEST_LIBRARIES} )
ADD_TEST( NAME plugintest COMMAND plugintest )
