{	"qphoenix" : {
		"module": {
			"type" : "plugin",
			"libname" : "${FULL_TARGET_NAME}",
			"info" : {
				"name" : "Scanner",
                                "version" : "${PROJECT_VERSION}",
                                "description" : "Scan selection buffer for text (X11 only feature)"
			}
		}
	}
}
